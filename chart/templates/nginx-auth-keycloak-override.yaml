{{- if eq .Values.auth.mode "keycloak_remote" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-auth-keycloak-override
data:
  nginx_auth_keycloak.conf: |
{{ .Files.Get "files/nginx_auth_keycloak_remote.conf" | indent 4 }}
{{- end }}
